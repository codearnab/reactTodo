/* eslint-disable no-unused-vars */
import React from 'react'

const TagButton = (Props) => {
  return (
    
      <button className="tag-btn">{Props.title}</button>
   
  )
}

export default TagButton
